# Core
numpy
pandas
scipy
torch
tqdm
pysam
matplotlib
seaborn

# Sequence utilities
tangermeme

# Model-specific dependencies. Install only what you need for the model(s) you
# plan to run. None of these are required for the dataset / plotting code.

# Enformer
enformer-pytorch

# Borzoi / Flashzoi
borzoi-pytorch

# NTv3
transformers

# AlphaGenome (PyTorch port). huggingface_hub is used to auto-download the
# `gtca/alphagenome_pytorch` all-folds weights when no local path is given.
alphagenome-pytorch
huggingface_hub

# Dev / tests (optional)
pytest
